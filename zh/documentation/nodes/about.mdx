---
title: "了解节点"
---

`AxiomLedger`网络由三种类型的节点组成：`验证者`，`出块者`和`候选者`，它们的主要差别是在`共识中的角色`：

- `验证者`将会参与共识投票；
- `出块者`是一类特殊的验证节点，在共识中会提出区块；
- `候选者`则不参与共识只是同步网络中最新的区块数据(也会转发交易)；

但在部署安装上它们没有任何区别。本章主要介绍如何单独部署一套开发环境网络以及如何部署节点参与到主网或测试网中。
