---
title: "eth_getBalance"
api: "POST /"
---

返回给定地址的帐户余额

### Body

<ParamField body="id" type="number" placeholder="1">
  请求标识符
</ParamField>

<ParamField body="jsonrpc" type="string" placeholder="2.0">
  正在使用的JSON-RPC版本
</ParamField>

<ParamField body="method" type="string" placeholder="eth_getBalance">
  进行调用的远程方法名称
</ParamField>

<ParamField body="params" type="array">
  包含传递给远程方法的参数列表

  <Expandable title="params">
     <ParamField body="address" type="string">
        表示要检查余额的地址（20字节）的字符串
     </ParamField>

     <ParamField body="block" type="string">
        十六进制块号，或字符串“latest”、“earliest”或“pending”，或块的哈希（32字节）
     </ParamField>

  </Expandable>
</ParamField>

### Response

<ResponseField name="id" type="number">
  返回请求的唯一标识符 
</ResponseField>

<ResponseField name="jsonrpc" type="string">
  返回正在使用的JSON-RPC版本
</ResponseField>

<ResponseField name="balance" type="string">
  当前余额的十六进制
</ResponseField>

<RequestExample>

```bash Example Request
curl  'https://rpc1.aries.axiomesh.io' \
--header 'Content-Type: application/json' \
--data '{
    "jsonrpc": "2.0",
    "method": "eth_getBalance",
    "params": ["0x2299E9B1a4e08b5d67017E19d75d3a24bb1074Df","latest"],
    "id": 1
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
    "jsonrpc": "2.0",
    "id": 1,
    "result": "0x0"
}
```

</ResponseExample>