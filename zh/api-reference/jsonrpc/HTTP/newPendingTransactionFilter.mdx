---
title: "eth_newPendingTransactionFilter"
api: "POST /"
---

在节点中创建一个筛选器，以便在新的待定交易到达时发出通知。要检查状态是否已更改，请调用eth_getFilterChanges。
### Body

<ParamField body="id" type="number" placeholder="1">
  请求标识符
</ParamField>

<ParamField body="jsonrpc" type="string" placeholder="2.0">
  正在使用的JSON-RPC版本
</ParamField>

<ParamField body="method" type="string" placeholder="eth_newPendingTransactionFilter">
  进行调用的远程方法名称
</ParamField>

<ParamField body="params" type="array">
  空
</ParamField>

### Response

<ResponseField name="id" type="number">
  返回请求的唯一标识符
</ResponseField>

<ResponseField name="jsonrpc" type="string">
  返回正在使用的JSON-RPC版本
</ResponseField>

<ResponseField name="filterId" type="string">
  过滤器id
</ResponseField>

<RequestExample>

```bash Example Request
curl   'https://example-rpc.axiomesh.io' \
--header 'Content-Type: application/json' \
--data '{
    "jsonrpc":"2.0",
    "method":"eth_newPendingTransactionFilter",
    "params":[],
    "id":1
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
    "id":1,
    "jsonrpc":  "2.0",
    "result": "0xc3ff65bb36e71e9c3fd6c362688c93e"
}
```

</ResponseExample>