---
title: "axm_syncProgress"
api: "POST /"
---

Returns sync progress of the node.


### Body

<ParamField body="id" type="number" placeholder="1">
  Serves as a unique identifier for the request
  
</ParamField>

<ParamField body="jsonrpc" type="string" placeholder="2.0">
  Indicates the version of JSON-RPC being used
</ParamField>

<ParamField body="method" type="string" placeholder="axm_syncProgress">
  Represents the name of the remote procedure or method to be called
</ParamField>

<ParamField body="params" type="array">
  Contains the list of parameters passed to the remote method
</ParamField>

### Response

<ResponseField name="id" type="number">
  Returns unique identifier for the request 
</ResponseField>

<ResponseField name="jsonrpc" type="string">
  Returns the version of JSON-RPC being used
</ResponseField>


<ResponseField name="syncProgress" type="object">
    A sync progress object

    <Expandable title="syncProgress object">
        <ResponseField name="inSync" type="bool">
            A boolean as false when not syncing or end syncing(sync all block but not catchUp in ledger)
        </ResponseField>

        <ResponseField name="catchUp" type="bool">
            A boolean as true when not syncing or end catching up in ledger
        </ResponseField>

        <ResponseField name="startSyncBlock" type="string">
            hex code of an integer indicating number of the block at which current sync term started
        </ResponseField>

        <ResponseField name="currentSyncHeight" type="string">
            hex code of an integer indicating number of the block at which current sync block height in this sync term
        </ResponseField>

        <ResponseField name="highestBlockHeight" type="string">
            hex code of an integer indicating number of the block at which current ledger's highest block height
        </ResponseField>

        <ResponseField name="currentSyncHeight" type="string">
            hex code of an integer indicating number of the block at which target height in current sync term
        </ResponseField>

        <ResponseField name="syncMode" type="string">
            indicating the specific sync mode, full or snapshot
        </ResponseField>

        <ResponseField name="peers" type="array">
            List of remote peers in this sync term
        </ResponseField>

    </Expandable>



</ResponseField>


<RequestExample>

```bash Example Request
curl  'https://example-rpc.axiomesh.io' \
--header 'Content-Type: application/json' \
--data '{
    "jsonrpc": "2.0",
    "method": "axm_syncProgress",
    "params": [],
    "id": 1
}'
```

</RequestExample>

<ResponseExample>

```json Response
    {
        "jsonrpc": "2.0",
        "id": 1,
        "result": {
        "inSync": true,
        "catchUp": false,
        "startSyncBlock": 219,
        "currentSyncHeight": 301,
        "highestBlockHeight": 218,
        "targetHeight": 1700,
        "syncMode": "full",
        "peers": [
    {
        "Id": 2,
        "PeerID": "16Uiu2HAmRypzJbdbUNYsCV2VVgv9UryYS5d7wejTJXT73mNLJ8AK"
    },
    {
        "Id": 3,
        "PeerID": "16Uiu2HAmTwEET536QC9MZmYFp1NUshjRuaq5YSH1sLjW65WasvRk"
    },
    {
        "Id": 4,
        "PeerID": "16Uiu2HAmQBFTnRr84M3xNhi3EcWmgZnnBsDgewk4sNtpA3smBsHJ"
    }
        ]
    }
    }
```

</ResponseExample>